import Filter from "../components/Filter";
import Graph from "../components/Graph";

const MainPage = () => {
  return (
    <section>
      <Filter />
      <Graph />
    </section>
  );
};

export default MainPage;
